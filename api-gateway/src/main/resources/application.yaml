server:
  port: 8888
spring:
  application:
    name: api-gateway

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/falcon-realm
          
  cloud:
    discovery:
      enabled: true
    config:
      enabled: false
    gateway:
      server:
        webflux:
          routes:
            - id: content_service_route # A unique string that identifies the route
              uri: lb://CONTENT-SERVICE # The target Uniform Resource Identifier where the request will be proxied
              predicates: # conditions that an incoming request must satisfy for the route to be considered a match
                - Path=/api/content/**
            - id: progression_service_route
              uri: lb://PROGRESSION-SERVICE
              predicates:
                - Path=/api/progression/**
            - id: instance_service_route
              uri: lb://INSTANCE-SERVICE
              predicates:
                - Path=/api/instance/**
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: false # the gateway will be reached by its hostname which will be resolved by docker DNS